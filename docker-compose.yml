services:
  nihility:
    image: ghcr.io/nihilityer/nihility:main
    network_mode: host
    volumes:
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
